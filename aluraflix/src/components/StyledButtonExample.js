import React from 'react';
import StyledButton from '../styles/StyledButton';

const StyledButtonExample = () => (
  <StyledButton>Botón Estilizado</StyledButton>
);

export default StyledButtonExample;
